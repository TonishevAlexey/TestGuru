<h1>Реестр тестов</h1>
<br>
<%= link_to 'Новый тест', new_admin_test_path %>
<table>
  <tr>
    <th>Название</th>
    <th>Уровень</th>
    <th>Категория</th>
    <th>Автор</th>
    <th>Кол-во вопросов</th>
    <th>Действия</th>
  </tr>
  <% @tests.each do |test| %>
    <tr>
      <td><%= test.title %></td>
      <td><%= test.level %></td>
      <td><%= test.category.title %></td>
      <td><%= test.author&.name %></td>
      <td><%= test.questions.count %></td>
      <td><%= link_to 'Просмотреть', admin_test_path(test) %>
        <%= link_to 'Удалить', admin_test_path(test), method: :delete, data: { confirm: 'Эй! Ты уверен?' } %>
        </tr>
  <% end %>
</table>