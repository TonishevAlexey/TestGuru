<h1>Новый вопрос к тесту <%=@test.title  %></h1>

<%= form_with model: [@test, @question], local: true do |form| %>

  <p>
    <%= form.text_field :body, id: :question_body %>
  </p>

  <p><%= form.submit value="Сохранить"%></p>
<% end %>
<%= link_to 'Вернуться', tests_path(@question.test_id) %>